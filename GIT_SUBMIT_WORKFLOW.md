# Youban 代码提交流程

以后在 Codex 中输入 **“提交今天代码”**，默认执行以下流程。

## 默认目标

- 仓库：`https://github.com/A6083450/youban.git`
- 远程：`origin`
- 分支：当前功能分支，禁止在 `main` 或 `master` 上自动提交
- 行为：检查、测试、提交并推送当前分支，不合并到主分支

## 自动执行步骤

1. 检查当前分支、远程仓库、最近一次提交和 GitHub 登录状态。
2. 查看所有已修改和未跟踪文件，确认本次提交范围。
3. 运行与改动相关的测试、构建检查和 `git diff --check`。
4. 暂存已确认的代码；新增文件必须逐个确认后暂存。
5. 再次检查暂存区文件、差异统计和敏感信息。
6. 根据实际改动生成简洁的 Conventional Commit 提交说明。
7. 使用 `git push origin <当前分支>` 推送，禁止强制推送。
8. 核对本地与远程提交哈希，并报告 GitHub 分支链接。

## 永不自动提交

- `.env`、API Key、Token、密码和其他凭据
- `.DS_Store`
- `data/` 运行数据
- `frontend/dist/`、测试报告等生成产物
- 与本次功能无关或来源不明的锁文件、临时文件

## 异常处理

- 当前分支是 `main` 或 `master`：停止并提示先切换功能分支。
- 测试失败：停止提交，先说明失败原因并处理。
- 远程仓库不匹配：停止推送，避免提交到错误仓库。
- 没有有效改动：不创建空提交，只报告当前已是最新状态。
- 发现敏感信息：不暂存、不提交，并明确指出文件位置。

该流程不会自动创建 Pull Request，也不会自动合并到 `main`。
