# Debug Journal — local frontend access and chat shell
Started: 2026-07-24T15:50:00+08:00
Goal: Make the frontend reachable at localhost:5173 and align the chat shell as a session list plus centered Agent-style conversation surface.

## Environment snapshot (Phase 0)
- Runtime: Vue 3 + Vite, Node/npm frontend
- Entry: `cd frontend && npm run dev`
- Ports / sockets: OrbStack owns `*:5173`; LAN URL returns HTTP 200, localhost resets the connection
- Git HEAD: 076a34d; working tree has user changes in `frontend/src/views/ChatHome.vue` and `frontend/vite.config.ts`
- References read: Node runtime, Playwright CLI, debugging setup/investigation, frontend design/redesign/perfection/layout, TypeScript, in-app Browser, Playwright skill

## Hypotheses
1. [OPEN] The host/container port path is bound in a way that resets loopback connections even though Vite listens on `0.0.0.0` — distinguishing evidence: direct `curl` and browser navigation results for localhost vs LAN IP — if true, fix is binding/forwarding.
2. [OPEN] The app shell reserves an unintended empty column or uses a non-centered flex/grid constraint, shifting the welcome/composer left — distinguishing evidence: computed rects and grid/flex styles in the live page — if true, fix is the shell's layout contract.
3. [OPEN] A fixed/absolute composer layer or parent overflow rule covers the intended chat viewport — distinguishing evidence: computed stacking/overflow and element hit-testing at the chat center — if true, fix is scroll ownership and stacking.

## Failed hypothesis round counter
- Round 1: pending runtime/browser capture.

## Artifacts to revert
- [ ] `/tmp/tripstar-localhost.headers` and `/tmp/tripstar-localhost.body` — created by the first curl probe; remove after evidence is captured.
- [ ] `/tmp/tripstar-lan.headers` and `/tmp/tripstar-lan.body` — created by the first curl probe; remove after evidence is captured.
- [ ] `/tmp/tripstar-playwright-*` — any browser QA captures created during this session; remove after evidence is recorded unless promoted as final evidence.
- [ ] `output/playwright/` — Playwright CLI visual captures; remove after evidence is recorded unless promoted as final evidence.
- [ ] `.debug-journal.md` — remove before final handoff; this is a local debugging ledger, not product source.

## Findings
- 2026-07-24T15:45:00+08:00 — `curl http://localhost:5173/` exited with `curl: (56) Recv failure: Connection reset by peer`; `curl http://192.168.156.2:5173/` returned HTTP 200 and an 806-byte HTML shell.
- 2026-07-24T15:46:00+08:00 — in-app Browser navigation to `http://localhost:5173/` reproduced `net::ERR_CONNECTION_RESET`.
- 2026-07-24T15:40:00+08:00 — user screenshot shows the main welcome/composer cluster left-shifted within the area right of the sidebar, with a large blank region to its right.

## Final fix
